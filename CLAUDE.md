# 项目协作约定 / CLAUDE.md

> 本文件记录与用户的长期协作约定。Claude Code 每次会话会自动读取本文件，
> 请在相关任务中默认遵循。

## 「做一个网站」的默认交付标准

当用户提出“做一个网站”（或类似建站需求）且**未特别说明范围**时，默认按 **全栈** 交付，
需覆盖以下 7 个层次。除非用户明确缩减范围，否则都应包含：

### 1. 前端页面
- 完整导航、首页、各业务栏目、数据展示页
- 联系 / 咨询表单
- PC + 移动端响应式适配

### 2. 后端项目（Django）
- 规范的 Django 项目 + 应用（app）结构
- settings / urls / views / 应用拆分清晰

### 3. 数据库（SQLite）
- 设计表结构与 Django 模型（models）
- 生成并执行迁移（makemigrations / migrate）
- 写入**真实数据**（非纯占位），具备可用的种子数据

### 4. 接口（前后端打通）
- 前端（Vue）表单 → 提交到 Django API
- 接口能真实接收并持久化数据，而非仅前端模拟

### 5. 后台管理
- 使用 Simple UI（django-simpleui）
- 中文后台界面
- 一级 / 二级菜单
- 数据的查询、列表展示、详情查看

### 6. 运行验证（必须实际执行，不能只口头声明）
- 启动后端服务
- 构建 / 运行前端
- 检查接口连通性
- **确认数据真实写入数据库**（实测，而非假设）

### 7. 项目文档
- 提供类似 `agent.md` 的项目说明文档：
  技术栈、目录结构、启动方式、接口说明、后台账号等

## 默认技术栈
- 后端：Django + SQLite + django-simpleui
- 前端：Vue + 响应式
- 验证：本地启动 + 接口自测 + 数据入库确认

## 备注
- 本仓库现有的「基智汇」站点为早期纯静态实现（HTML/CSS/JS），
  尚未包含上述后端 / 数据库 / 接口 / 后台层。如需，可按本标准升级为全栈版本。
- 运行环境为临时容器，**仅提交进仓库的内容会持久化**，重要产物务必提交。
