# 项目协作约定 / CLAUDE.md

> 本文件记录与用户的长期协作约定。Claude Code 每次会话会自动读取本文件，
> 请在相关任务中默认遵循。

## 「做一个网站」的默认交付标准

当用户提出“做一个网站”（或类似建站需求）且**未特别说明范围**时，默认按 **全栈** 交付，
需覆盖以下 7 个层次。除非用户明确缩减范围，否则都应包含：

### 1. 前端页面
- 完整导航、首页、各业务栏目、数据展示页
- 联系 / 咨询表单
- PC + 移动端响应式适配

### 2. 后端项目（Django）
- 规范的 Django 项目 + 应用（app）结构
- settings / urls / views / 应用拆分清晰

### 3. 数据库（SQLite）
- 设计表结构与 Django 模型（models）
- 生成并执行迁移（makemigrations / migrate）
- 写入**真实数据**（非纯占位），具备可用的种子数据

### 4. 接口（前后端打通）
- 前端（Vue）表单 → 提交到 Django API
- 接口能真实接收并持久化数据，而非仅前端模拟

### 5. 后台管理
- 使用 Simple UI（django-simpleui）
- 中文后台界面
- 一级 / 二级菜单
- 数据的查询、列表展示、详情查看

### 6. 运行验证（必须实际执行，不能只口头声明）
- 启动后端服务
- 构建 / 运行前端
- 检查接口连通性
- **确认数据真实写入数据库**（实测，而非假设）

### 7. 项目文档
- 提供类似 `agent.md` 的项目说明文档：
  技术栈、目录结构、启动方式、接口说明、后台账号等

## 默认技术栈
- 后端：Django + SQLite + django-simpleui
- 前端：Vue + 响应式
- 验证：本地启动 + 接口自测 + 数据入库确认

## 代码质量原则（强制）
- **严禁打补丁式修复**。任何修改 / 修复 / 新增都必须从根源解决，遵循最佳实践，
  通过重构、调整或与现有逻辑结合的方式深度融合到代码内部。
- 不为了快速满足某个需求而堆叠特例、绕过问题或留下死代码。
- 追求**结果的简洁与完整**：不是改动过程简单，而是最终代码以最少、最清晰的实现
  完整覆盖需求；移除冗余、占位与残留。

## 验收标准（凡涉及前端 / 后端的任务都必须执行）
- **前端**：使用端到端工具（如 **Playwright**）对**桌面端 + 移动端**同时验收。
  不能只截图，必须把业务链路带入具体复杂场景，按用户流程逐步操作
  （点按钮、填输入、跳转等）完整走查，既验 UI 也验 UX。场景含两类：
  1）正常流程走通；2）边界 / 异常情况，验证能否如预期触发并被拦截。
- **后端**：
  - 非 AI 接口：做预期内的「正常结果」与「异常结果」测试。
  - AI 接口：带入复杂任务场景，测试**决策能力**（提示词 / 上下文 / 约束能否驱动
    AI 完成复杂任务）与**执行能力**（工具执行后能否得到 AI 预期结果）。
- 模块级任务前后端都要测。
- **秉持「找茬」思想**：目标是验证它**有没有错**，而非证明它对——不仅查布局，
  也查内容（文案、数据是否契合当前场景），主动找出错误。

## 备注
- 「基智汇」已按本标准升级为**全栈版**：`backend/`（Django+DRF+SQLite+Simple UI）
  + `frontend/`（Vue3+Vite），真实数据入库、接口打通、中文后台、联系表单落库、
  Playwright 桌面+移动端到端验收齐备；详见根目录 `AGENT.md`。
- 根目录早期纯静态站（`index.html`/`css`/`js`）保留为零依赖的轻量演示版。
- 运行环境为临时容器，**仅提交进仓库的内容会持久化**，重要产物务必提交。
